name: cd-workflow
run-name: Operator deployment triggered by ${{ github.actor }}
on:
  workflow_run:
    workflows:
      - ci-build
    branches:
      - main
    types:
      - completed
jobs:
  CD:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: self-hosted
    steps:
      - name: Checkout the code
        uses: actions/checkout@v6

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: 'latest'

      - name: Set kubeconfig
        run: |
          echo "Setting up the kubeconfig"
          mkdir -p $HOME/.kube
          echo "${{ secrets.CD_KUBECONFIG }}" | base64 -d > $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      - name: Create the operator namespace
        run: |
          kubectl create namespace staticwebsite-operator \
          --dry-run=client -o yaml | kubectl apply -f -

      - name: Deploy the registry pull secret
        run: |
          kubectl create secret docker-registry staticwebsite-operator \
            --docker-username=${{ vars.DOCKERHUB_USERNAME }} \
            --docker-password=${{secrets.DOCKERHUB_TOKEN}} \
            --docker-server=https://index.docker.io/v1/ \
            --namespace=staticwebsite-operator \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Deploy the operator
        run: |
          echo "Changing the context for staticwebsite-operator namespace"
          kubectl config set-context --current --namespace=staticwebsite-operator
          
          echo "Applying the operator CRD manifest"
          kubectl apply -f kubernetes/crd/ 
          
          echo "Applying the kubernetes RBAC manifests"
          kubectl apply -f kubernetes/rbac/
          
          echo "Applying the operator deployment manifest"
          kubectl apply -f kubernetes/deployment/

      - name: Verify operator rollout
        run: |
          kubectl rollout status deployment/staticwebsite-operator \
          -n staticwebsite-operator \
          --timeout=120s
          
          
          
